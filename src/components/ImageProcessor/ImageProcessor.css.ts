import { recipe } from '@vanilla-extract/recipes';

export const ImageProcessorCanvasStyle = recipe({
  base: {
    width: 160,
    border: '1px solid black',
    boxShadow: '5px 5px 5px rgba(0,0,0,0.25)',
  },
});
